{
  "entities": {
    "MaterialItem": {
      "title": "Material or Piece Item",
      "description": "Specific piece or material used in the service",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" }
      },
      "required": ["id", "name", "price"]
    },
    "ServicePiece": {
      "title": "Service Piece",
      "description": "Details of a specific car piece being serviced",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "label": { "type": "string" },
        "selected": { "type": "boolean" },
        "description": { "type": "string" },
        "audioBlob": { "type": "string", "description": "Base64 encoded audio" }
      },
      "required": ["id", "label", "selected"]
    },
    "Note": {
      "title": "Service Order Note",
      "description": "The main service order document",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string", "description": "Owner of the note" },
        "customerName": { "type": "string" },
        "vehicleNameColor": { "type": "string" },
        "plate": { "type": "string" },
        "cpfCnpj": { "type": "string" },
        "whatsapp": { "type": "string" },
        "status": { "type": "string", "enum": ["em_espera", "na_oficina", "finalizado"] },
        "deliveryDate": { "type": "string", "format": "date" },
        "pieces": {
          "type": "array",
          "items": { "$ref": "#/entities/ServicePiece" }
        },
        "includePartsValue": { "type": "boolean" },
        "partsValue": { "type": "number" },
        "includeLaborValue": { "type": "boolean" },
        "laborValue": { "type": "number" },
        "includeMaterialsValue": { "type": "boolean" },
        "materialsValue": { "type": "number" },
        "onlyTotalValue": { "type": "boolean" },
        "totalValue": { "type": "number" },
        "materialItems": {
          "type": "array",
          "items": { "$ref": "#/entities/MaterialItem" }
        },
        "observations": { "type": "string" },
        "isDraft": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "customerName", "status", "createdAt", "updatedAt"]
    },
    "Assinatura": {
      "title": "Subscription",
      "description": "User subscription status",
      "type": "object",
      "properties": {
        "status": { "type": "string", "enum": ["ativo", "inativo"] }
      },
      "required": ["status"]
    }
  },
  "firestore": {
    "notes": {
      "schema": "Note",
      "description": "Collection of all service orders"
    },
    "assinaturas": {
      "schema": "Assinatura",
      "description": "Collection of user subscriptions"
    }
  }
}
